randomizes skills in skill trees across classes

uses Mopioids seed system

each seed works for all 4 characters

to set up the mod, enable it in console and use the menus to generate a seed, then go and select the seed.

then youre all set.
